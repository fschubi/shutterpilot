╔═══════════════════════════════════════════════════════════╗
║   SHUTTERPILOT v0.3.4 + CARD v2.1.0 - INSTALLATION      ║
╚═══════════════════════════════════════════════════════════╝

🐛 PROBLEM GELÖST:
  ✓ Config-Sensor wird jetzt korrekt erstellt
  ✓ Feste Entity-ID: sensor.shutterpilot_config
  ✓ Profile werden in Card angezeigt
  ✓ Speichern funktioniert

─────────────────────────────────────────────────────────────

📦 SCHRITT 1: DATEIEN KOPIEREN

Windows PowerShell:

  # Backend
  xcopy "C:\dev\shutterpilot\custom_components\shutterpilot\*" ^
        "\\wsl$\homeassistant\config\custom_components\shutterpilot\" /E /Y

  # Card
  copy "C:\dev\shutterpilot\www\shutterpilot-card.js" ^
       "\\wsl$\homeassistant\config\www\shutterpilot-card.js" -Force

─────────────────────────────────────────────────────────────

🔄 SCHRITT 2: HOME ASSISTANT NEU STARTEN

  Einstellungen → System → Neu starten

  ⚠️ WICHTIG: Komplett neu starten, NICHT nur Integration!

─────────────────────────────────────────────────────────────

🌐 SCHRITT 3: BROWSER-CACHE LEEREN

  Strg + Shift + R (mehrmals drücken!)

─────────────────────────────────────────────────────────────

✅ SCHRITT 4: VERIFIZIEREN

1. Config-Sensor prüfen:
   
   Entwicklertools → States → Suche: "shutterpilot_config"
   
   ✓ Sollte gefunden werden: sensor.shutterpilot_config
     State: OK
     Attributes: {entry_id, profiles, areas, global_settings}

2. Card-Console prüfen:
   
   F12 → Console
   
   ✓ "SHUTTERPILOT-CARD Version 2.1.0"
   ✓ "✅ Config Sensor geladen: {...}"
   ✓ "✅ 3 Profile geladen: [...]"

3. Profile anzeigen:
   
   Card öffnen → Tab "Profile"
   
   ✓ Alle in Integration konfigurierten Profile sichtbar

─────────────────────────────────────────────────────────────

🧪 SCHRITT 5: TESTEN

1. Neues Profil erstellen:
   
   Card → "+ Neues Profil" → Ausfüllen → Speichern
   
   Erwartetes Verhalten:
   ✓ Toast: "Profil erstellt"
   ✓ Toast: "Konfiguration gespeichert"
   ✓ Nach 1 Sek.: Profil in Tabelle

2. Console prüfen:
   
   F12 → Console
   
   Erwartete Logs:
   ✓ "💾 Speichere Config: {profiles: X, areas: Y}"
   ✓ "✅ Service-Call erfolgreich"

3. Backend-Log prüfen:
   
   Einstellungen → System → Logs
   
   Erwartete Logs:
   ✓ "update_config service called with X profiles and Y areas"
   ✓ "Config entry updated, will reload automatically"

─────────────────────────────────────────────────────────────

❌ FALLS PROBLEME:

1. Config-Sensor nicht gefunden:
   → HA KOMPLETT neu starten (nicht nur reload!)
   → Prüfe: Settings → Devices → ShutterPilot → Entities
   → Sensor sollte AKTIVIERT sein

2. Profile nicht sichtbar:
   → Prüfe: sensor.shutterpilot_config → Attributes → profiles
   → Falls leer: In Integration über ConfigFlow Profile hinzufügen

3. Speichern funktioniert nicht:
   → F12 → Console → Fehler prüfen
   → Settings → System → Logs → Fehler suchen
   → Service prüfen: Entwicklertools → Services → "shutterpilot.update_config"

─────────────────────────────────────────────────────────────

📖 DETAILLIERTE ANLEITUNG:

  Siehe: QUICKFIX_v0.3.4.md

─────────────────────────────────────────────────────────────

✨ NEUE FEATURES:

Backend v0.3.4:
  • Config-Sensor mit fester Entity-ID
  • Sensor standardmäßig aktiviert
  • Automatische Updates bei Config-Änderungen
  • Ausführliche Debug-Logs

Card v2.1.0:
  • Explizite Sensor-Suche
  • Bessere Fehlermeldungen
  • Debug-Logs für Troubleshooting
  • Zeigt verfügbare Sensoren bei Fehler

─────────────────────────────────────────────────────────────

💡 WICHTIG:

  Der Config-Sensor (sensor.shutterpilot_config) ist das "Bindeglied"
  zwischen Integration und Card:
  
  Integration → Config-Sensor (Attributes) → Card
  
  Die Card liest IMMER aus dem Sensor, niemals direkt aus der Config!

─────────────────────────────────────────────────────────────

🎯 ERWARTETES ERGEBNIS:

  ✓ Card zeigt alle Profile aus Integration
  ✓ Neues Profil erstellen funktioniert
  ✓ Profile bearbeiten funktioniert
  ✓ Speichern funktioniert
  ✓ Nach HA-Neustart bleiben Profile erhalten
  ✓ Keine Console-Fehler

─────────────────────────────────────────────────────────────

